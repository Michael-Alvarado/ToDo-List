<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device=width, initial-scale=1.0">
    <title>Family ToDo List</title>
    <link rel="stylesheet" href="/appMonster/public/css/style.css">
    <link rel="stylesheet" href="/appMonster/node_modules/bootstrap/dist/css/bootstrap.min.css"
</head>

<body>
    <header class="display-flex justify-space-between align-center">
        <h1>
            Family ToDo List
        </h1>

        <nav>
            <button class="btn" id="login">Login</button>
            {{!-- Only display the logout button if the user is logged in --}}
            {{#if logged_in}}
            <button class="btn" id="logout">Logout</button>        
            {{/if}}
        </nav>
    </header>
    
    <main class="container container=fluid">
        {{{ body }}}
    </main>

    <footer>

    </footer>

    {{#if logged_in}}
    <script src="/appMonster/public/js/logout.js"></script>
    {{/if}}
</body>

</html>